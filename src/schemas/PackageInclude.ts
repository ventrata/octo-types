// Generated by ts-to-zod
import { z } from 'zod';
import { includeSchema } from './Include';
import { type PackageInclude } from '../models/PackageInclude';

export const packageIncludeSchema: z.ZodType<PackageInclude> = z.lazy(() =>
	z.object({
		title: z.string().optional(),
		count: z.number(),
		includes: z.array(includeSchema),
	}),
);
